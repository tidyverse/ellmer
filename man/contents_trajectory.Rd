% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content-trajectory.R
\name{contents_trajectory}
\alias{contents_trajectory}
\title{Convert a conversation into a trajectory-v1 record}
\usage{
contents_trajectory(content, ...)
}
\arguments{
\item{content}{A \link{Chat}, \link{Turn}, or \link{Content} object to convert into a
trajectory-v1 record.}
}
\value{
A plain R list matching the \code{trajectory-v1} schema. When \code{content}
is a \link{Chat}, the result is the full ordered list of records (starting
with \code{meta}); when \code{content} is a \link{Turn} or \link{Content}, the result is the
list of records generated by that turn (or content fragment) alone.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{contents_trajectory()} serializes a \link{Chat}, \link{Turn}, or \link{Content} object
into a plain R list matching the shape of the letta-ai \code{trajectory-v1} JSON
schema: an ordered list of records describing the \code{user}, \code{assistant},
\code{reasoning}, and \code{tool} events in a conversation, preceded by a \code{meta}
record.

Call \code{jsonlite::toJSON(x, auto_unbox = TRUE, null = "null")} on the result
to obtain the JSON text for the trajectory. \code{null = "null"} is required so
that assistant tool-call records (which carry \code{content = NULL}) serialize
their \code{content} field as JSON \code{null}, matching the schema, rather than
having it dropped.

The \code{trajectory-v1} schema allows each record to carry a \code{timestamp}.
ellmer does not track per-turn timestamps, except when the provider itself
reports one; currently this is only the case for OpenAI's Responses API.
When no timestamp is available, the field is omitted rather than
synthesized.
}
\examples{
turns <- list(
  UserTurn(list(
    ContentText("What's this image?"),
    content_image_url("https://placehold.co/200x200")
  )),
  AssistantTurn("It's a placeholder image.")
)

lapply(turns, contents_trajectory)

}
