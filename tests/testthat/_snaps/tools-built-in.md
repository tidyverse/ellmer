# claude_tool_code_execution() rejects a non-string type

    Code
      claude_tool_code_execution(123)
    Condition
      Error in `claude_tool_code_execution()`:
      ! `type` must be a single string, not the number 123.

